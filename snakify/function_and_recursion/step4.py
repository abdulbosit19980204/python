# Explanations-2
# We'd like to provide another example. Here's the function max() that accepts two numbers and returns the maximum of them
# Actually, this function has already become the part of Python syntax

def reMux(a, b):
    if a > b:
        return a
    elif a < b:
        return b
    else:
        return "Ikkala qiymat ham teng"


print(reMux(5, 5))
print(reMux(3, 5))
print(reMux(7, 2))

